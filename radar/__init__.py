"""Radar de licitaciones.

La versión se compara con la etiqueta de la última release publicada en GitHub para
saber si hay algo más nuevo (ver `radar/actualizacion.py`). Al publicar una release hay
que subir este número **y** etiquetarla igual: si no coinciden, el actualizador se niega
a instalarla, que es justo lo que se quiere cuando el paquete no es lo que dice ser.
"""

__version__ = "1.1.0"
